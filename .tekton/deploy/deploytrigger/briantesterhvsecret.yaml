apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: "briantester-external-secret"
spec:
  secretStoreRef:
    name: briantester-vault
    kind: SecretStore
  refreshInterval: "0s"
  target:
    name: prodsecrets
    creationPolicy: 'Owner'
    deletionPolicy: "Delete"
  data:
  - secretKey: repoGithubToken
    remoteRef:
      key: generic/project/otc-setup/disconnected-agent/repoGithubToken
      property: token
